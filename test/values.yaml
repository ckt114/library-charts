test:
  serviceName: MyService

  extraObjects:
    - apiVersion: external-secrets.io/v1
      kind: ExternalSecret
      metadata:
        name: '{{ include "common.names.fullname" . }}'

    - apiVersion: external-secrets.io/v1
      kind: ConfigMap
      metadata:
        name: '{{ include "common.names.fullname" . }}'
